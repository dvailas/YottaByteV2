<h1>Checkout</h1>
<h4>Subtotal: $<%=@subtotal%></h4>
<h2>Taxes:</h2>
<h4>GST: $<%=(@subtotal*@prov.GST).round(2) %></h4>
<h4>PST: $<%=(@subtotal*@prov.PST).round(2) %></h4>
<h4>HST: $<%=(@subtotal*@prov.HST).round(2) %></h4>
<h3>Total: $<%=(@subtotal +(@subtotal*@prov.GST)).round(2) %></h3>
<p><%= link_to 'Make Purchase', make_order_product_index_path, method: :post%></p>
<div class ="ui grid">
  <%= render partial: 'product', collection: @cart,
          locals: {show_link: true,cart: true,checkout:true}%>
</div>